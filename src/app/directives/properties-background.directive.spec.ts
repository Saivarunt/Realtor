import { PropertiesBackgroundDirective } from './properties-background.directive';

describe('PropertiesBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new PropertiesBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
