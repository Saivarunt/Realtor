import { SellerPermissionsDirective } from './seller-permissions.directive';

describe('SellerPermissionsDirective', () => {
  it('should create an instance', () => {
    const directive = new SellerPermissionsDirective();
    expect(directive).toBeTruthy();
  });
});
